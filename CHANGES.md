# Что было добавлено

## 1. MSSQL Server 2019 в Docker Compose

В `docker-compose.yaml` добавлен сервис:
- **Образ**: `mcr.microsoft.com/mssql/server:2019-latest`
- **Порт**: 1433
- **База данных**: `Examen_ModelFirst`
- **Логин**: sa
- **Пароль**: YourStrong!Passw0rd
- **Healthcheck**: Проверка готовности БД перед запуском приложения
- **Volume**: `mssql-data` для сохранения данных

## 2. Обновлены строки подключения

В `Examen/Server/appsettings.Docker.json`:
- Server изменен с `host.docker.internal` на `mssql` (имя контейнера)
- User изменен с `user1` на `sa`
- Password изменен на `YourStrong!Passw0rd`

## 3. Добавлены скрипты инициализации

- `Examen/SQL/init-db.sh` - bash скрипт для создания БД
- `Examen/SQL/init-db.sql` - SQL скрипт инициализации

## 4. Добавлены PowerShell скрипты

- `docker-start.ps1` - Быстрый запуск всех сервисов с проверками
- `docker-stop.ps1` - Остановка сервисов с опцией удаления данных

## 5. Документация

- `DOCKER_SETUP.md` - Подробная инструкция по работе с Docker

## Как запустить

### Вариант 1: PowerShell скрипт
```powershell
.\docker-start.ps1
```

### Вариант 2: Docker Compose напрямую
```bash
docker-compose up -d
```

## Подключение к БД

**С хоста (Windows):**
- Server: `localhost,1433`
- Database: `Examen_ModelFirst`
- User: `sa`
- Password: `YourStrong!Passw0rd`

**Из контейнера:**
- Server: `mssql`
- Database: `Examen_ModelFirst`
- User: `sa`
- Password: `YourStrong!Passw0rd`

## Примечания

⚠️ **ВАЖНО**: В production обязательно смените пароль SA!

🔄 Examen Server автоматически дождется готовности MSSQL благодаря `depends_on` с условием `service_healthy`.

💾 Данные БД сохраняются в Docker volume и не удаляются при остановке контейнеров.
